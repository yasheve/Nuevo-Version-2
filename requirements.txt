fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
PyJWT>=2.8
argon2-cffi>=23.1
python-multipart>=0.0.9

# Managed Postgres driver (Neon / RDS) — REQUIRED in production:
psycopg[binary]>=3.1

# S3-compatible object storage (this build uses app/storage.py -> S3 / R2 / B2):
boto3>=1.34

# Live nameplate OCR (set ANTHROPIC_API_KEY to enable; harmless if unset):
anthropic>=0.39

# Excel (.xlsx) register export — formatted workbook generation:
openpyxl>=3.1
